# Legacy Long-Press Component

This page documents a historical repo residue so it does not get confused with
the active runtime.

## What It Is

The repository still contains files under `src/singnote/components/` for a
custom long-press note editor experiment.

## Current Status

- not used by the active home page
- not part of the current deployment path
- replaced in practice by native Streamlit editing controls

## Why This Page Exists

Without this note, a newcomer might assume the component is still active
because the files are present. It is not.
