# Kakes Synitheies - Version 2 Notes

Source: https://kithara.to/stixoi/MzE1OTU1NzE1/kakes-synitheies-pasxalidis-miltiadis-lyrics

## Identity

- Song: `Kakes Synitheies`
- Artist: `Miltos Paschalidis`
- Kithara submitter: `Greekrocker`
- Submitted: `2007-05-30`
- Kithara rating at capture: `4.1 / 5`, `68 votes`
- Detected key: `G major`
- Diatonic set shown by source: `G`, `Am`, `Bm`, `C`, `D`, `Em`, `F#dim`
- Includes tablature/riff material directly in the chart.

## Structural Read

This version is more detailed than Version 1. It writes out the intro and repeats more explicitly:

- intro starts with `G G4 G G4`, then moves through repeated `G Am C D`
- intro later returns to `G G4 G G4`
- verse lines are given with repeated chord rows, making the harmonic placement more explicit
- the long opening lyric line sits over `G Am C D G`
- the second verse phrase expands through `G Am C D C D`
- the bad-habits section uses `Am Em D G D`
- a small `D` riff is printed after that section
- refrain uses `C D G Bm Em`
- inner refrain line uses `Am D B7 Em`
- ending line uses `Am D G`
- the same general structure repeats for verse 2

## Chord Ideas Worth Keeping

- `G G4 G G4` is the clearest intro color from this version.
- `Am Em D G D` is a strong candidate for the "bad habits / old things" section.
- `C D G Bm Em` appears again as the refrain backbone.
- `Am D B7 Em` gives the refrain a stronger dominant pull into `Em` than Version 1.
- `Am D G` is a simpler closing tag than Version 1's `Am D C G`.

## Comments And Crowd Corrections

- `agelos` says the basic melody is right, but suggests `Am7` instead of `C` around the "too late for truths" ending.
- `agelos` also suggests a richer ending: `Am7 D C/Em7/Am7/D7 G`.
- `NicholasG9` suggests that the ending around the cafe/cigarette phrase may sound better with `D C G`.
- One anonymous commenter says this version is the strongest and asks that other confusing versions be removed.
- `ZAXAPIAS` says to use `D7` where it sounds right and agrees this is the best of the versions.
- `N.` explicitly says this is finally the correct version, but suggests a small change in the refrain: instead of `Bm`, pass through the note `F#` and then go to `Em`. This supports treating `Bm` as a disputed/passing color rather than a locked chord.
- `NikGR` gives a direct positive validation: "perfect rendering, thank you."
- `greekrocker` credits a music-theory teacher for deriving the version, which is useful context but not proof by itself.
- Several comments discuss `G4`, with one clarification that it means `Gsus4`.
- Some comments focus on solo/tab details; useful later only if we decide to add an instrumental learning section.

## Rhythm Hypothesis

This page does not solve strumming directly, but its chord layout implies more precise phrase-level changes than Version 1:

- intro should probably be played with ringing `G` to `Gsus4` movement
- verse can use the same folk-rock pulse, but the chord changes are more frequent
- `Am Em D G D` section needs careful split-bar timing
- `B7` in the refrain should be treated as a short color chord, not a place to slow down

## Reliability Notes

This version has fewer votes but a higher score and stronger internal detail. It is probably better for the final SingNote harmony skeleton than Version 1, especially for intro color, the `Am Em D G D` section, and the `B7` movement. Comments also treat it as a more correct version. The main caution is that even supporters suggest local refinements around `Bm`, `Am7`, `D7`, and the final tag.

## Open Questions

- Should `G4` be stored in YAML as `Gsus4` for clarity?
- Should the final tag stay simple as `Am D G`, use Version 1's `Am D C G`, or use the richer commenter ending?
- Should `Am7` replace `C` in the "too late for truths" phrase?
- Should `D7` be added as an optional color in the General tab notes?
